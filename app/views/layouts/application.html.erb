<!DOCTYPE html>
<html>
<head>
<title>RJHS Diversity Day Registration</title>
<%= stylesheet_link_tag    "application", :media => "all" %>
<%= javascript_include_tag "application" %>
<%= csrf_meta_tags %>
<script type="text/javascript">
$(document).ready(function () {
    if(window.location.href.indexOf("72") > -1) {
    window.navigate("http://diversity.regisjesuit.com");
    }
    });
</script>
<%= yield(:head) %>
</head>
<body>

<style type="text/css">
#info { position: absolute; right: 5%; top: 0px; }
.flash { z-index: 3; text-align: right; position: relative; top: 30px; left: -5%; font-size: 1.1em;}
#yielded { position: absolute; top: 125px; left: 10%; }
</style>

<div id="wrapper">
  <div id="header">
    <div style="position: absolute; left: 10%;">
      <a href="/" tab="0" did="0" pid="192" ><%= image_tag "/assets/rjhs-logo.png" %></a>
    </div>

    <% if user %>
      <p id="info">Logged in as <%= user.full %>.  <%= link_to "Log out", "/user/logout" %></p>
  <% end %>
    <% if flash[:warning] %>
      <p id="flash_warning" class="flash" style="color: #859900;"><%= flash[:warning] %></p>
  <% end %>
    <% if flash[:notice] %>
      <p id="flash_notice" class="flash"><%= flash[:notice] %></p>
  <% end %>
    <% if flash[:error] %>
      <p id="flash_error" class="flash"style="color: #dc322f;"><%= flash[:error] %></p>
  <% end %>
  </div>

</div>
<div id="yielded">
  <%= yield %>
</div>
<div style="clear: both;"></div>
</body>
</html>
