<h1>Step 2: Register for Sites</h1>

<p style="width: 500px;"></p>

<%= form_tag(:controller => "user", :action => "register_event") do %>

<% unless session[:user].event_id == 0 %>
<p>Currently registered for site: <%= Event.find(:first, :conditions => ["id=?", session[:user].event_id]).name %></p>
<% else %>
<p>Not currently registered for a site.</p>
<% end %>
<label for="sessionid">Please choose a site:</label><br/>

<%= select("event", "sessionid", ["--Unregister--"].concat(Event.all.sort { |a, b|  a.name <=> b.name }.collect {|e| [ "#{e.name} - #{e.users.count}/#{e.maxcapacity}", e.id ] }), { :include_blank => true }) %>

<br />

<%= submit_tag("Submit")%>

<br />
<br />
<br />

<% unless session[:user].event_id == 0 %>
<p><strong>You're done!</strong>  You can now safely <%= link_to "log out", "/user/logout"%>.</p>
<p>For detailed information about your service site, please consult the appropriate <strong>RJHS Service Project Handbook</strong>, a link to which can be found at the bottom of this page.</p>
<% end %>

<% end %>

